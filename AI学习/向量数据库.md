# 向量数据库



## 📘 一、Milvus 简介

- **类型**：开源的向量数据库（Vector Database）
- **适用场景**：语义检索、图像搜索、问答系统、推荐系统、大模型记忆体等
- **支持向量类型**：Dense Vector（稠密向量）
- **数据维度支持**：通常几十到几千维

------

## 🧱 二、核心概念



| 名称          | 说明                                |
| ------------- | ----------------------------------- |
| Collection    | 类似“表”（存储一个向量集合）        |
| Field         | 类似“列”，可以是向量、ID、标签等    |
| Index         | 向量索引（如 IVF_FLAT, HNSW, etc.） |
| Search        | 查询最相似的向量（支持 TopK）       |
| Insert        | 插入向量数据                        |
| Delete        | 删除向量                            |
| Hybrid Search | 支持向量 + 结构化字段一起查         |

------

## 🛠 三、环境安装

### 方法一：使用 Docker（推荐）

```bash

docker pull milvusdb/milvus:v2.3.4
```

使用官方 docker-compose 快速启动：

```bash
git clone https://github.com/milvus-io/milvus.git
cd milvus/deployments/docker-compose
docker-compose up -d
```

### 方法二：裸机部署

参考官方文档：https://milvus.io/docs

------

## 🔧 四、Python SDK 使用

Milvus 的 Python SDK 是 `pymilvus`：

```bash

pip install pymilvus
```

### 示例代码：

```python
from pymilvus import connections, FieldSchema, CollectionSchema, DataType, Collection

# 连接服务器
connections.connect("default", host="localhost", port="19530")

# 创建 Collection
fields = [
    FieldSchema(name="id", dtype=DataType.INT64, is_primary=True, auto_id=False),
    FieldSchema(name="embedding", dtype=DataType.FLOAT_VECTOR, dim=128)
]
schema = CollectionSchema(fields, description="Test collection")
collection = Collection("my_collection", schema)

# 插入数据
import numpy as np
ids = list(range(10))
vectors = np.random.rand(10, 128).tolist()
collection.insert([ids, vectors])

# 创建索引
collection.create_index(field_name="embedding", index_params={"index_type": "IVF_FLAT", "metric_type": "L2", "params": {"nlist": 100}})

# 查询
collection.load()
results = collection.search(vectors[:1], "embedding", param={"nprobe": 10}, limit=5)
for r in results[0]:
    print(f"id: {r.id}, distance: {r.distance}")
```

------

## 📌 五、推荐学习路径

1. ✅ Milvus 入门教程
2. ✅ 跑通 Python SDK 示例
3. ✅ 学习向量索引类型（IVF、HNSW、FLAT 等）
4. ✅ 尝试 RAG 应用场景 + LLM（如结合 LlamaIndex、LangChain）
5. ✅ 学习持久化、删除、分区、标签查询等高级功能

------

如果你告诉我你的目标（比如：记忆体、文档问答、图像搜索），我可以给你定制一份 Milvus 实战计划或者 demo 哦！你目前用它是想干什么？
---

---

# 1.Vue3学习路线

### **1. 基础入门**

####  **了解 Vue 3 的核心概念**

- Vue 3 介绍与对比 Vue 2
- Vue 3 开发环境搭建（Node.js、Vue CLI、Vite）
- 创建 Vue 3 项目（使用 Vue CLI 或 Vite）

####  **学习 Vue 3 的基本语法**

- 模板语法（插值、指令 `v-if`、`v-for`、`v-bind`、`v-model` 等）
- 事件绑定（`@click`、`@change` 等）
- 计算属性（`computed`）与侦听器（`watch`、`watchEffect`）
- 生命周期钩子（`onMounted`、`onUpdated` 等）

####  **Composition API**（Vue 3 重点）

- `setup` 函数
- `ref` 和 `reactive` 的区别
- `computed` 与 `watch`
- `provide` / `inject`
- 自定义 Hook（封装逻辑复用）

------

### **2. 进阶学习**

 **组件系统**

- 父子组件通信（`props` & `emit`）
- `defineProps` 和 `defineEmits`（Vue 3 语法糖）
- `v-model` 在组件中的使用
- 插槽（默认插槽、具名插槽、作用域插槽）

 **Vue Router 4（前端路由）**

- 路由基础（动态路由、嵌套路由、命名路由）
- 路由传参（query、params）
- 路由守卫（`beforeEach`、`afterEach`）
- KeepAlive 和懒加载

**Pinia（状态管理）**（推荐取代 Vuex）

- `setup()` 和 `options API` 方式的使用
- `defineStore` 创建状态
- `state`、`getters`、`actions`
- 持久化存储（`pinia-plugin-persistedstate`）

 **Vue 3 响应式系统**

- `reactive`、`ref`、`shallowRef`
- `readonly` 和 `toRefs`
- `computed` 与 `watchEffect`

**Vue 3 动画**

- `transition` 组件（入场/离场动画）
- `transition-group`（列表动画）
- Vue 3 的 `useTransitionState`

------

### **3. 高级进阶**

 **Vue 3 的性能优化**

- 编译优化（静态提升、PatchFlags）
- 懒加载与异步组件
- 事件代理与列表渲染优化
- `v-memo` 和 `v-once`

 **Vue 3 + TypeScript**

- 组合 API + TypeScript
- `defineProps` 和 `defineEmits` 类型定义
- `shims-vue.d.ts` 配置

**SSR（服务端渲染）和 Nuxt 3**

- Nuxt 3 介绍与项目搭建
- 服务端渲染（SSR）和静态生成（SSG）

**前端工程化**

- ESLint + Prettier 代码规范
- Husky + Lint-staged 代码提交规范
- Vite 配置优化（按需加载、CDN）

------

### **4. 实战项目**

 **搭建一个完整的 Vue 3 项目**

- Vue 3 + Vite 开发环境
- Vue Router 进行页面导航
- Pinia 管理全局状态
- Axios 封装 API 请求
- Element Plus / Ant Design Vue 组件库
- 部署（Vercel / Nginx / Docker）

------

### **5. 学习资源推荐**

📌 **官方文档**：

- Vue 3 文档：https://vuejs.org/
- Vue Router 4 文档：https://router.vuejs.org/
- Pinia 文档：https://pinia.vuejs.org/

📌 **视频教程 & 博客**

- B 站：Vue 3 教程（黑马、尚硅谷、掘金等）
- GitHub 项目（element-plus-admin、vue3-admin-template）



# 2.基础组件名称定义

应用都是由一棵嵌套的、可重用的组件树组成的。

eg：一个待办事项 (Todos) 应用的组件树可能是这样的

```
App (root component)
├─ TodoList
│  └─ TodoItem
│     ├─ TodoDeleteButton
│     └─ TodoEditButton
└─ TodoFooter
   ├─ TodoClearButton
   └─ TodoStatistics
```

### **📌 组件分析**

#### **🌱 根组件**

- ```
  App
  ```

  （根组件）

  - 负责管理全局状态（例如 `todos` 数据列表）。
  - 提供状态和方法给子组件（例如 `TodoList`、`TodoFooter`）。

#### **📝 任务列表部分**

- `TodoList`
  - 负责渲染 `todos` 数组，并将每个 `todo` 传递给 `TodoItem`。
- `TodoItem`（单个待办项）
  - 显示单个 `todo`，并提供 **编辑** 和 **删除** 按钮。
  - 接收 `todo` 数据，并向父组件传递事件（如 `onDelete`、`onEdit`）。
- `TodoDeleteButton`
  - 负责删除该 `todo`，点击后触发删除事件。
- `TodoEditButton`
  - 允许用户修改 `todo` 的文本内容。

#### **📊 底部统计部分**

- `TodoFooter`
  - 负责显示 Todo 相关的统计数据，并提供 **清空** 功能。
- `TodoClearButton`
  - 提供按钮，点击后清空所有 `todo`。
- `TodoStatistics`
  - 计算并显示统计信息（如完成的任务数 / 总任务数）。

### **📌 Vue 3 代码示例**

#### 1️⃣ **`App.vue`（根组件）**

```vue
<script setup>
import { ref } from 'vue'
import TodoList from './components/TodoList.vue'
import TodoFooter from './components/TodoFooter.vue'

const todos = ref([
  { id: 1, text: 'Learn Vue 3', completed: false },
  { id: 2, text: 'Build a project', completed: true }
])

const deleteTodo = (id) => {
  todos.value = todos.value.filter(todo => todo.id !== id)
}

const editTodo = (id, newText) => {
  const todo = todos.value.find(t => t.id === id)
  if (todo) todo.text = newText
}

const clearTodos = () => {
  todos.value = []
}
</script>

<template>
  <div>
    <h1>Todo App</h1>
    <TodoList :todos="todos" @delete-todo="deleteTodo" @edit-todo="editTodo" />
    <TodoFooter :todos="todos" @clear-todos="clearTodos" />
  </div>
</template>
```

------

#### 2️⃣ **`TodoList.vue`**

```vue
<script setup>
import TodoItem from './TodoItem.vue'
defineProps(['todos'])
defineEmits(['deleteTodo', 'editTodo'])
</script>

<template>
  <ul>
    <TodoItem 
      v-for="todo in todos" 
      :key="todo.id" 
      :todo="todo"
      @delete-todo="$emit('deleteTodo', todo.id)"
      @edit-todo="$emit('editTodo', todo.id, $event)"
    />
  </ul>
</template>
```

------

#### 3️⃣ **`TodoItem.vue`**

```vue
<script setup>
import TodoDeleteButton from './TodoDeleteButton.vue'
import TodoEditButton from './TodoEditButton.vue'

defineProps(['todo'])
defineEmits(['deleteTodo', 'editTodo'])
</script>

<template>
  <li>
    <span>{{ todo.text }}</span>
    <TodoEditButton :todo="todo" @edit-todo="$emit('editTodo', todo.id, $event)" />
    <TodoDeleteButton @delete-todo="$emit('deleteTodo', todo.id)" />
  </li>
</template>
```

------

#### 4️⃣ **`TodoDeleteButton.vue`**

```vue
defineEmits(['deleteTodo'])
</script>

<template>
  <button @click="$emit('deleteTodo')">❌ 删除</button>
</template>
```

------

#### 5️⃣ **`TodoEditButton.vue`**

```vue
import { ref } from 'vue'

const props = defineProps(['todo'])
const emit = defineEmits(['editTodo'])

const newText = ref(props.todo.text)

const updateTodo = () => {
  emit('editTodo', newText.value)
}
</script>

<template>
  <input v-model="newText" />
  <button @click="updateTodo">✏️ 编辑</button>
</template>
```

------

#### 6️⃣ **`TodoFooter.vue`**

```vue
<script setup>
import TodoClearButton from './TodoClearButton.vue'
import TodoStatistics from './TodoStatistics.vue'

defineProps(['todos'])
defineEmits(['clearTodos'])
</script>

<template>
  <div>
    <TodoStatistics :todos="todos" />
    <TodoClearButton @clear-todos="$emit('clearTodos')" />
  </div>
</template>
```

------

#### 7️⃣ **`TodoClearButton.vue`**

```vue
<script setup>
defineEmits(['clearTodos'])
</script>

<template>
  <button @click="$emit('clearTodos')">🗑 清空任务</button>
</template>
```

------

#### 8️⃣ **`TodoStatistics.vue`**

```vue
import { computed } from 'vue'

const props = defineProps(['todos'])

const completedCount = computed(() => props.todos.filter(todo => todo.completed).length)
const totalCount = computed(() => props.todos.length)
</script>

<template>
  <p>✅ 完成任务: {{ completedCount }} / {{ totalCount }}</p>
</template>
```

------

### **📌 总结**

🔹 通过 **组件拆分**，使代码更清晰，每个组件职责单一。
 🔹 使用 **`props` 传递数据**，**`emit` 触发事件**，父组件处理逻辑。
 🔹 使用 **Vue 3 Composition API**，提高代码复用性和组织性。

这就是一个完整的 **Vue 3 Todo 应用**！🚀
 如果你有其他需求，比如 **Pinia 状态管理、后端 API、动画效果**，也可以进一步扩展！ 🎯